# site properties and page defaults
title: DUCK LAB
subtitle: 
description:  description of  lab
#header: images/background.jpg
#footer: images/background.jpg
proofer: false


# REPO SETTINGS
# crucial: this must match "Org/Repo" exactly
repository: The-DUCK-Lab/The-DUCK-Lab.github.io

# URL SETTINGS
# crucial: baseurl must be empty quotes "" for an organisation root site
url: "https://the-duck-lab.github.io"
baseurl: ""

# site social media and other links
links:
#  email: e.giunchiglia@ic.ac.uk
#  orcid: 0000-0001-8713-9213
#  google-scholar: ETJoidYAAAAJ
  github: duck-AI-lab
  x: duck-AI-lab
#  youtube: duck-AI-lab

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
